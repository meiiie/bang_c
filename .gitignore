output*/
out-*/
traces*/
run*/
mtp_server_logs/
task-runs/
task-runs-*/
eval-runs/
data/
build/
dist/
.venv/
.wrangler/
*.egg-info/
*.log
*.trace.jsonl
models/
*.gguf
*.safetensors
__pycache__/
*.pyc
.pytest_cache/
CLAUDE.local.md

# Secrets — never commit. HF_TOKEN is passed to the Docker build as a BuildKit
# secret (env or file); keep any token file out of the tree.
.env
.env.*
hf_token.txt
*_token.txt

# Development-only contest scratch artifacts may contain public-test prompts,
# answer keys, or proxy-reference outputs. Keep narrative notes tracked.
notes/_*.json
notes/_*.txt
notes/*public463*preflight*.json
notes/*measured-gate*.json
notes/*experiment-assessment*.json
# Public-leaderboard probe tooling + human-review matrices (group-testing scratch).
scripts/probe_decoder.py
docs/*human-review*/
output-probes/
