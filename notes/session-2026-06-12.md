# Session report — 2026-06-12 (Neko Core, HackAIthon Bảng C)

Single authoritative record for this session. Supersedes the per-topic 06-12 notes
(reasoning-token-sweep, quant-direction, direction-2-vn-rag, pseudo-label-analysis), whose
results are inlined below. Active follow-ups keep their own notes: `mtp-direction-2026-06-12.md`
(resume plan) and `runpod-setup-gotchas.md` (pod reference). Durable rules: `lessons.md`.

## 1. Headline

The shipped contestant is **Gemma-4 26B-A4B (MoE), Q4_0, self-consistency CoT k=1 @ 2048
tokens + safety lever**, measured leaderboard band **88.55**. This session hardened the
submission, measured and **closed two speculative accuracy levers** (higher-precision quant,
maj@k voting), confirmed the time/precision knobs, and scoped the one remaining upside (MTP,
a lossless speed lever for the Vòng-2 Time score). All GPU pods terminated; nothing billing.

## 2. Shipped this session (committed, 211 tests green)

| Deliverable | Commit | Note |
|---|---|---|
| **Bulletproof `pred.csv` contract** — `repair_predictions_for_contract` + write-before-validate; a solver gap / bad letter / contract miss can never zero the run. Docker-CMD smoke PASSED on real GPU (contract 40/40). | `607f244` | highest-value Vòng-2 robustness fix |
| **Idea doc (EN)** rewritten to measured state | `7a2af8c` | `docs/method-writeup.md` |
| **RunPod setup gotchas** quick-reference | `d932e92` | `notes/runpod-setup-gotchas.md` |
| **GPU-run playbook + lessons** (env-path, double-run, health-verify, cost-first cloud) | `08ddec1`,`b758a37`,`2334035` | `notes/lessons.md` |

## 3. Measured results

### 3a. reasoning_max_tokens sweep (RTX 4090, full 463) → keep 2048
| tokens | time 463 | s/q | priv-2000 est | vs 91.79 ref |
|---|---|---|---|---|
| 768 | 21.2 min | 2.75 | ~1.53 h | 92.01% |
| 1280 | 22.1 min | 2.87 | ~1.59 h | 92.87% |
| **2048** | 22.6 min | 2.93 | ~1.63 h | **93.09%** |

Time is nearly flat (2048 only +6.7% over 768 — the MoE early-stops before the cap binds), so
the cap is effectively free and 2048 is weakly dominant. **Decision: keep 2048.**

### 3b. Quantization Q4 vs Q6 vs Q8 (A40, full 463) → keep Q4_0, lever DEAD
| quant | vs 91.79 ref | vs Q4 (net on differing) | time | VRAM |
|---|---|---|---|---|
| **Q4_0 (shipped, QAT)** | **93.30%** | — | 22 min | 15 GB |
| Q6_K | 92.66% | net −3 | 40 min | 22 GB |
| Q8_0 | 93.09% | net −1 | 42 min | 28 GB |

Q6/Q8 are tied-to-slightly-WORSE than Q4 at 2× time+VRAM. Our Q4_0 is **QAT** (trained for
4-bit → minimal loss); the research's "−4..−10pp 4-bit" is POST-training quant, a different
thing. The math-proxy quant gap does NOT transfer. **Decision: keep Q4_0.**

### 3c. Levers measured DEAD (do not re-spend)
- **maj@k / k=5 diverse voting** — WASH (FPT n=120/bucket: 89.7 vs 90.0 k=1). Gemma's errors
  are SYSTEMATIC; diverse samples agree on the same wrong answer, so voting just confirms it.
- **higher-precision quant** — see 3b.
- **dense 31B** — ~90 s/q (vs 26B ~2.9 s/q); ~50 h on 2000 priv → forfeits Time for ~0.7–2pp.
- Prior: TIR, reading-grounding, RAG, few-shot, tiered, UD-Q4_K_XL, Qwen3.5-9B standalone.

→ Both speculative accuracy levers are now exhausted. **88.55 is near the honest offline
ceiling**; the gap to the 91.79 frontier reference is knowledge-bound (VN-2025 admin facts,
local trivia, ≥6 defective golds), not technique-addressable cheaply.

## 4. Active task — MTP (Multi-Token Prediction): the Vòng-2 TIME lever

**What:** speculative decoding — the model drafts tokens, the main model verifies → output is
identical, **zero accuracy loss**, purely **1.4–2.2× faster** (Gemma-4 QAT+MTP 1.5–2.2×). It
is orthogonal to quant; ship both. This directly buys the 10-pt Time score for free.

**Progress (this session got the pipeline ~90% there):**
- ✅ Exact recipe obtained (unsloth README + llama.cpp PR #23398).
- ✅ llama.cpp CLI built with CUDA on the pod; main QAT Q4 (14 G) + MTP draft
  `mtp-gemma-4-26B-A4B-it.gguf` (441 M) downloaded.
- ✅ **GPU offload SOLVED** (Session 2): CUDA0 seen, model loads ~20 GB on GPU.
- ⛔ **BLOCKER (current):** llama.cpp master (b1-d8a24cc) `llama-cli` is an interactive chat REPL
  that won't do a clean one-shot generation (`-no-cnv` and `-st` both fail → infinite `> ` loop).
  Right path = **llama-server** (HTTP, also the Docker `local_server` provider); it needs a
  `tools/ui/ui.cpp` zero-size-array patch (applied) or a stable tag. Measurement cancelled by owner.

**Resume (next session, ~25 min):** full detail in `notes/mtp-direction-2026-06-12.md` §Session 2.
1. Skip `llama-cli` one-shot benchmarking; it was the tooling blocker, not the production path.
2. Use `llama-server` via `scripts/gpu/run_mtp_server.sh` after owner sign-off for RunPod spend.
3. Keep full server logs; pull `/workspace/mtp_server_logs/` before terminating.
4. Measure baseline vs `--spec-type draft-mtp --spec-draft-n-max 1/2/4/6`, **KV cache f16**
   (q8 KV → 0% acceptance risk), record tok/s + draft acceptance.
- Old temp assets remain in `C:/Users/Admin/AppData/Local/Temp/pod_mtp/`, but the tracked
  server script is now the canonical entrypoint.
- Docker wiring path once measured: `local_server` provider (llama-server supports
  `--spec-type draft-mtp`) or an upgraded `llama-cpp-python`.

## 5. Open decisions for the owner

- **Vòng-1 team-nick (bar ~88.98):** error-structure analysis of the 31 disagreements
  (`notes/error-analysis-31-disagreements-2026-06-12.md`) shows the bar **IS reachable in
  principle** — 16/463 confirmed-recoverable (+3.46pp ceiling vs ~2 net flips needed) — but the
  recoverable set is **deterministic calc + reasoning** (Henderson-Hasselbalch, motor RPM,
  econ-geography), i.e. what the **already-built TIR/router path targets, NOT the Qwen
  adjudicator** (verified coin-flip/backfire). 13 are unrecoverable knowledge gaps → 88.55 is
  near the honest offline ceiling.
- **If chasing 88.98:** ONE cheap GPU run of the `router` strategy (TIR on) on the real 463 vs
  the 88.55 baseline — ship only if net-positive (calc recoveries > false flips on the 27
  ref-right). NOT the Qwen adjudicator (don't spend on it).
- **Recommended default:** lock 88.55, pour effort into **Vòng-2** (MTP time lever + bulletproof
  Docker + Idea doc). Chase 88.98 only if the owner wants the margin (one router measurement).

## 6. Housekeeping
- All RunPod pods terminated (0 running). RunPod key `Downloads/runpod987.txt`.
- Reusable orchestration: `pod26/` (sweep), `pod26q/` (quant), `pod_mtp/` (MTP) under
  `C:/Users/Admin/AppData/Local/Temp/`. Cost-first cloud rule (community OK if cheaper).
